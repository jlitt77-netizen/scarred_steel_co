# Database connection. SQLite for local/dev; swap for a Postgres URL in production.
DATABASE_URL="file:./dev.db"

# 32+ char secret used to sign session cookies (JWT). MUST be overridden in production.
SESSION_SECRET="change-me-to-a-long-random-string-at-least-32-chars"

NODE_ENV="development"
